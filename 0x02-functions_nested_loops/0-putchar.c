#include <main.h>

/**
 * main - Print _putchar
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char word = "_putchar";

	for (word = _putchar;)
	{
		putchar(word);
	}
	putchar('\n');
	return (0);
}

