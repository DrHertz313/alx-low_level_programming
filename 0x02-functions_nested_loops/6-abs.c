#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *@n: the int to check
 * Return: the absolute value of int n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
