#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: input
 * Return: 1 if c is uppercase, 0 if otherwise
 */
int _isupper(int c)
{
	for ((c >= 'A') && (c <= 'Z'))

		return (1);
	else
		return (0);
}
